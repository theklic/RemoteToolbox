# Pinned-ish runtime deps for `pip install -r requirements.txt`.
# Prefer `pip install -e .` (reads pyproject.toml) for development.
#
# Core:
httpx>=0.27
pydantic>=2.6
pyyaml>=6.0
python-dotenv>=1.0

# Optional frontends / integrations — uncomment what you use:
# python-telegram-bot>=21.0   # Telegram chat adapter
# mcp>=1.0                     # connect to external MCP servers
